configuration: [--no-loggers, --no-compositions, --debug, -opng]
default_deployment: avalon
default_robot: avalon

base:
    deployment: avalon

# Everything in this section is tested separately
simple_tests:
    - pose_estimator
    - orientation_estimator
    - testing
    - pipeline
    - pipeline_detector
    - drive_simple
    - buoy
    - buoy_detector
    - wall
    - wall_detector


